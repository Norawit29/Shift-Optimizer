## Packages
framer-motion | Smooth animations for wizard steps and cards
recharts | Visualizing fairness metrics and shift distribution
date-fns | Date manipulation for calendar generation
clsx | Utility for conditional classes (often needed with Tailwind)
tailwind-merge | Merging Tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
  mono: ["'Fira Code'", "monospace"],
}
