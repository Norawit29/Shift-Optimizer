We will improve per-shift balance without changing target calculation.

Do NOT modify how staffShiftTargets is computed.
Keep:

target ∝ availability

We will instead add a soft per-shift spread minimization term.

🔧 Modify buildDistributionModel() only.

After this line:

this.writeLoadTrackingConstraints(constraintLines, varMap, cIdx);

and before the objective section, add:

// --- Per-shift spread tracking (between staff for same shift) ---
// This minimizes max(ts_i_s) - min(ts_i_s) softly
// without touching availability-based targets.

for (let s = 0; s < S; s++) {
  const hasTarget = this.staff.some((_, i) => staffShiftTargetsInt[s][i] > 0);
  if (!hasTarget) continue;

  const maxVar = `maxSL_${s}`;
  const minVar = `minSL_${s}`;

  for (let i = 0; i < N; i++) {
    if (staffShiftTargetsInt[s][i] === 0) continue;

    // ts_i_s <= maxSL_s
    constraintLines.push(`  c${cIdx.val++}: ts_${i}_${s} - ${maxVar} <= 0`);

    // ts_i_s >= minSL_s
    constraintLines.push(`  c${cIdx.val++}: - ts_${i}_${s} + ${minVar} <= 0`);
  }
}
🔧 Then modify the objective section

Inside the objective construction (after the SHIFT_W ds_i_s terms, and before holiday terms), add:

// Soft per-shift spread penalty
// Must be much smaller than SHIFT_W (1000)

const SHIFT_RANGE_W = 50;

for (let s = 0; s < S; s++) {
  const hasTarget = this.staff.some((_, i) => staffShiftTargetsInt[s][i] > 0);
  if (!hasTarget) continue;

  objParts.push(`+ ${SHIFT_RANGE_W} maxSL_${s}`);
  objParts.push(`- ${SHIFT_RANGE_W} minSL_${s}`);
}
🔧 Add bounds for new variables

Inside the Bounds section, add:

for (let s = 0; s < S; s++) {
  const hasTarget = this.staff.some((_, i) => staffShiftTargetsInt[s][i] > 0);
  if (!hasTarget) continue;

  lines.push(`  maxSL_${s} >= 0`);
  lines.push(`  minSL_${s} >= 0`);
}
❗ Important Constraints

Do NOT:

Change target calculation

Change Phase 2A

Change bestRange logic

Change solver settings

Add hard lower bounds

Modify SHIFT_W

Modify holiday logic

🎯 Expected Effect

Total load range (19–20) remains protected by Phase 2A

Availability fairness remains intact

Per-shift distribution between staff becomes smoother

No infeasibility risk

Minimal runtime increase